//  Controller -> Manipulates modal and view


//  Controll images

function next(source)
{
    carosel.innerHTML = /*HTML*/`
    <img src="${source}" alt="${description}" class="css-img">`;
}

function prev(source)
{
    carosel.innerHTML = /*HTML*/`
    <img src="${source}" alt="${description}" class="css-img">`;
}

// Controll text


// Controll buttons
